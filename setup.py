from setuptools import setup, find_packages

setup(
    name="dns_log_analysis",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
    ],
)